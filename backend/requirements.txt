# Exact pins so the Railway build is deterministic — a rebuild triggered by an
# unrelated push can never pick up a brand-new dep release and fail. Verified
# on Python 3.11 (see .python-version). To upgrade: bump deliberately, boot the
# app, then commit the new pins.
fastapi==0.141.1
uvicorn[standard]==0.39.0
pydantic==2.13.4
pydantic-settings==2.15.0
supabase==2.31.0
# Required by FastAPI to parse multipart/form-data — the document upload
# endpoint (POST /tasks/{id}/document) uses UploadFile. Without it the app
# crashes on startup (import-time) and Railway's healthcheck fails.
python-multipart==0.0.20
