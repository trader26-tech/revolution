# Pinned lower bounds are the versions verified in tests; upper caps keep a
# future breaking major from silently failing the Railway build.
fastapi>=0.128,<0.200
uvicorn[standard]>=0.39,<0.40
pydantic>=2.13,<3.0
pydantic-settings>=2.11,<3.0
supabase>=2.31,<3.0
phonenumbers>=9.0,<10.0
httpx>=0.28,<0.29
