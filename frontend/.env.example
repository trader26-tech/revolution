# Orbit frontend configuration
#
# Point this at the running Orbit FastAPI backend to enable cloud sync.
# Leave it empty (or omit the file) to run fully local (localStorage only).
#
# Copy to `.env.local` and edit:
#   cp .env.example .env.local

VITE_API_BASE_URL=http://localhost:8000
