// Copy this file's placeholders and fill in your real values.
// For production, use environment.prod.ts (git-ignored if it holds secrets)
// or inject values at build time.
export const environment = {
  production: false,
  supabaseUrl: 'https://your-project-ref.supabase.co',
  supabaseKey: 'your-anon-key',
  apiUrl: 'http://localhost:8000',
};
