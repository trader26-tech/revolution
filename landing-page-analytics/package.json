{
  "name": "revolution-landing-page-analytics",
  "version": "1.0.0",
  "description": "Deploy root — installs and starts the backend server.",
  "scripts": {
    "postinstall": "cd backend && npm install",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18"
  }
}
